import { KeyInputDirective } from './key-input.directive'

describe('KeyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyInputDirective()
    expect(directive).toBeTruthy()
  })
})
