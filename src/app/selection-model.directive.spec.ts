import { SelectionModelDirective } from './selection-model.directive'

describe('SelectionModelDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionModelDirective()
    expect(directive).toBeTruthy()
  })
})
